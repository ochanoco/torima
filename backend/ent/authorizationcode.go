// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent/dialect/sql"
	"github.com/ochanoco/proxy/ent/authorizationcode"
	"github.com/ochanoco/proxy/ent/serviceprovider"
)

// AuthorizationCode is the model entity for the AuthorizationCode schema.
type AuthorizationCode struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Token holds the value of the "token" field.
	Token string `json:"token,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the AuthorizationCodeQuery when eager-loading is set.
	Edges                                AuthorizationCodeEdges `json:"edges"`
	service_provider_authorization_codes *int
}

// AuthorizationCodeEdges holds the relations/edges for other nodes in the graph.
type AuthorizationCodeEdges struct {
	// Owner holds the value of the owner edge.
	Owner *ServiceProvider `json:"owner,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// OwnerOrErr returns the Owner value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e AuthorizationCodeEdges) OwnerOrErr() (*ServiceProvider, error) {
	if e.loadedTypes[0] {
		if e.Owner == nil {
			// Edge was loaded but was not found.
			return nil, &NotFoundError{label: serviceprovider.Label}
		}
		return e.Owner, nil
	}
	return nil, &NotLoadedError{edge: "owner"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*AuthorizationCode) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case authorizationcode.FieldID:
			values[i] = new(sql.NullInt64)
		case authorizationcode.FieldToken:
			values[i] = new(sql.NullString)
		case authorizationcode.ForeignKeys[0]: // service_provider_authorization_codes
			values[i] = new(sql.NullInt64)
		default:
			return nil, fmt.Errorf("unexpected column %q for type AuthorizationCode", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the AuthorizationCode fields.
func (ac *AuthorizationCode) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case authorizationcode.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			ac.ID = int(value.Int64)
		case authorizationcode.FieldToken:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field token", values[i])
			} else if value.Valid {
				ac.Token = value.String
			}
		case authorizationcode.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field service_provider_authorization_codes", value)
			} else if value.Valid {
				ac.service_provider_authorization_codes = new(int)
				*ac.service_provider_authorization_codes = int(value.Int64)
			}
		}
	}
	return nil
}

// QueryOwner queries the "owner" edge of the AuthorizationCode entity.
func (ac *AuthorizationCode) QueryOwner() *ServiceProviderQuery {
	return (&AuthorizationCodeClient{config: ac.config}).QueryOwner(ac)
}

// Update returns a builder for updating this AuthorizationCode.
// Note that you need to call AuthorizationCode.Unwrap() before calling this method if this AuthorizationCode
// was returned from a transaction, and the transaction was committed or rolled back.
func (ac *AuthorizationCode) Update() *AuthorizationCodeUpdateOne {
	return (&AuthorizationCodeClient{config: ac.config}).UpdateOne(ac)
}

// Unwrap unwraps the AuthorizationCode entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (ac *AuthorizationCode) Unwrap() *AuthorizationCode {
	_tx, ok := ac.config.driver.(*txDriver)
	if !ok {
		panic("ent: AuthorizationCode is not a transactional entity")
	}
	ac.config.driver = _tx.drv
	return ac
}

// String implements the fmt.Stringer.
func (ac *AuthorizationCode) String() string {
	var builder strings.Builder
	builder.WriteString("AuthorizationCode(")
	builder.WriteString(fmt.Sprintf("id=%v, ", ac.ID))
	builder.WriteString("token=")
	builder.WriteString(ac.Token)
	builder.WriteByte(')')
	return builder.String()
}

// AuthorizationCodes is a parsable slice of AuthorizationCode.
type AuthorizationCodes []*AuthorizationCode

func (ac AuthorizationCodes) config(cfg config) {
	for _i := range ac {
		ac[_i].config = cfg
	}
}

// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/ochanoco/database/ent/authorizationcode"
	"github.com/ochanoco/database/ent/serviceprovider"
)

// AuthorizationCodeCreate is the builder for creating a AuthorizationCode entity.
type AuthorizationCodeCreate struct {
	config
	mutation *AuthorizationCodeMutation
	hooks    []Hook
}

// SetToken sets the "token" field.
func (acc *AuthorizationCodeCreate) SetToken(s string) *AuthorizationCodeCreate {
	acc.mutation.SetToken(s)
	return acc
}

// SetOwnerID sets the "owner" edge to the ServiceProvider entity by ID.
func (acc *AuthorizationCodeCreate) SetOwnerID(id int) *AuthorizationCodeCreate {
	acc.mutation.SetOwnerID(id)
	return acc
}

// SetNillableOwnerID sets the "owner" edge to the ServiceProvider entity by ID if the given value is not nil.
func (acc *AuthorizationCodeCreate) SetNillableOwnerID(id *int) *AuthorizationCodeCreate {
	if id != nil {
		acc = acc.SetOwnerID(*id)
	}
	return acc
}

// SetOwner sets the "owner" edge to the ServiceProvider entity.
func (acc *AuthorizationCodeCreate) SetOwner(s *ServiceProvider) *AuthorizationCodeCreate {
	return acc.SetOwnerID(s.ID)
}

// Mutation returns the AuthorizationCodeMutation object of the builder.
func (acc *AuthorizationCodeCreate) Mutation() *AuthorizationCodeMutation {
	return acc.mutation
}

// Save creates the AuthorizationCode in the database.
func (acc *AuthorizationCodeCreate) Save(ctx context.Context) (*AuthorizationCode, error) {
	var (
		err  error
		node *AuthorizationCode
	)
	if len(acc.hooks) == 0 {
		if err = acc.check(); err != nil {
			return nil, err
		}
		node, err = acc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*AuthorizationCodeMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = acc.check(); err != nil {
				return nil, err
			}
			acc.mutation = mutation
			if node, err = acc.sqlSave(ctx); err != nil {
				return nil, err
			}
			mutation.id = &node.ID
			mutation.done = true
			return node, err
		})
		for i := len(acc.hooks) - 1; i >= 0; i-- {
			if acc.hooks[i] == nil {
				return nil, fmt.Errorf("ent: uninitialized hook (forgotten import ent/runtime?)")
			}
			mut = acc.hooks[i](mut)
		}
		v, err := mut.Mutate(ctx, acc.mutation)
		if err != nil {
			return nil, err
		}
		nv, ok := v.(*AuthorizationCode)
		if !ok {
			return nil, fmt.Errorf("unexpected node type %T returned from AuthorizationCodeMutation", v)
		}
		node = nv
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (acc *AuthorizationCodeCreate) SaveX(ctx context.Context) *AuthorizationCode {
	v, err := acc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (acc *AuthorizationCodeCreate) Exec(ctx context.Context) error {
	_, err := acc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (acc *AuthorizationCodeCreate) ExecX(ctx context.Context) {
	if err := acc.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (acc *AuthorizationCodeCreate) check() error {
	if _, ok := acc.mutation.Token(); !ok {
		return &ValidationError{Name: "token", err: errors.New(`ent: missing required field "AuthorizationCode.token"`)}
	}
	return nil
}

func (acc *AuthorizationCodeCreate) sqlSave(ctx context.Context) (*AuthorizationCode, error) {
	_node, _spec := acc.createSpec()
	if err := sqlgraph.CreateNode(ctx, acc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	return _node, nil
}

func (acc *AuthorizationCodeCreate) createSpec() (*AuthorizationCode, *sqlgraph.CreateSpec) {
	var (
		_node = &AuthorizationCode{config: acc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: authorizationcode.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: authorizationcode.FieldID,
			},
		}
	)
	if value, ok := acc.mutation.Token(); ok {
		_spec.SetField(authorizationcode.FieldToken, field.TypeString, value)
		_node.Token = value
	}
	if nodes := acc.mutation.OwnerIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: true,
			Table:   authorizationcode.OwnerTable,
			Columns: []string{authorizationcode.OwnerColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: serviceprovider.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.service_provider_authorization_codes = &nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// AuthorizationCodeCreateBulk is the builder for creating many AuthorizationCode entities in bulk.
type AuthorizationCodeCreateBulk struct {
	config
	builders []*AuthorizationCodeCreate
}

// Save creates the AuthorizationCode entities in the database.
func (accb *AuthorizationCodeCreateBulk) Save(ctx context.Context) ([]*AuthorizationCode, error) {
	specs := make([]*sqlgraph.CreateSpec, len(accb.builders))
	nodes := make([]*AuthorizationCode, len(accb.builders))
	mutators := make([]Mutator, len(accb.builders))
	for i := range accb.builders {
		func(i int, root context.Context) {
			builder := accb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*AuthorizationCodeMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, accb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, accb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, accb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (accb *AuthorizationCodeCreateBulk) SaveX(ctx context.Context) []*AuthorizationCode {
	v, err := accb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (accb *AuthorizationCodeCreateBulk) Exec(ctx context.Context) error {
	_, err := accb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (accb *AuthorizationCodeCreateBulk) ExecX(ctx context.Context) {
	if err := accb.Exec(ctx); err != nil {
		panic(err)
	}
}
